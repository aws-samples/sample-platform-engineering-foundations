# Example instance: the entire multi-tier application in ~10 lines.
# Replace s3Bucket and bucketSuffix with your own values before applying.
apiVersion: kro.run/v1alpha1
kind: ServerlessMultiTier
metadata:
  name: app-020
spec:
  name: app-020
  s3Bucket: REPLACE_WITH_LAMBDA_BUCKET
  s3Key: function-python-users-posts.zip
  size: medium
  environment: dev
  bucketSuffix: REPLACE_WITH_ACCOUNT_ID
